; Just using a Collatz sequence as an example file to parse.
; This syntax is not yet finalised and may change at any time.
x := 642789
i := 0
[
    x & 1 ? [
        x = x * 3 + 1
        i++
    ] : [
        x >>= 1
    ]
] x /= 1 ??